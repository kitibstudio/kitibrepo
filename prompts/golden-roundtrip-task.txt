You are running UNATTENDED. No human will answer questions. Behave accordingly:
when you would normally ask, you STOP instead — that is the safe action, and a
stopped run is a successful outcome, not a failure.

PRECEDENCE: Drift protection outranks improvement. Always. If you are unsure
whether you may change something, you may not. Ambiguity resolves toward MORE
restriction, never less.

═══ READ FIRST ═══
CLAUDE.md, state/CURRENT.md, state/DECISIONS.md (D19, D26, D28, D29, D33),
state/PARKED.md, state/FEATURES.md, state/REJECTED.md,
Tests/Fixtures/golden-documents/README.md, and scripts/validate.sh as the model
of a real gate. Do not survey the rest of the codebase.

═══ STEP 0 — THREE CARRY-OVERS FROM YOUR LAST PASS, FIRST ═══
Small, do them before the main task and commit them separately.

1. D33 says a scan finding zero files exits non-zero. That rule has never been
   observed firing. Prove it: `mkdir -p /tmp/kitib-empty && ./scripts/defect-corpus.sh
   /tmp/kitib-empty; echo "exit=$?"`. A directory that EXISTS and holds no
   .md/.txt is the D33 case; "nonexistent directory" is a different path and does
   not test it. If it exits 0, fix it. Paste the output into the commit.
2. Evidence the debris cleanup: `git status --short Tests/Fixtures/defect-corpus/`
   (must show nothing) and `ls -l Tests/Fixtures/defect-corpus/` (must show three
   files, mode 644). Then prove the stray-file guard fires: create a dummy file
   there, run gate mode, confirm it FAILS, delete the dummy, confirm it passes
   again. A guard never observed firing is indistinguishable from no guard.
3. state/FEATURES.md still needs to describe what was actually proved about
   defect-corpus.sh, not what the first pass claimed. Correct it if it is stale.

═══ THE MAIN TASK ═══
Acceptance criterion 10: clean input is a no-op. A golden document corpus now
exists at Tests/Fixtures/golden-documents/ — five documents and a README, hand
authored by the human's assistant, NEVER generated by running the code. Read
that README before you touch anything: it states what the corpus deliberately
avoids and why.

The work splits in two, and the split is the point (D26):

A. THE ROUND TRIP ITSELF NEEDS THE SWIFT, SO IT IS AN XCTEST, NOT A SHELL SCRIPT.
   Create Tests/KitibTests/GoldenDocumentTests.swift asserting, for every .md
   file in the corpus, that PasteHealer.heal(document) == document, byte for
   byte. Also assert the corpus is non-empty and that the file count matches what
   is on disk, so a corpus that silently stops being found fails rather than
   passes vacuously. Follow the pattern in
   Tests/KitibTests/PasteHealing/FixtureCorpus.swift for locating files.

B. scripts/golden-roundtrip.sh CHECKS CORPUS INTEGRITY WITHOUT RUNNING ANY SWIFT.
   It must NOT invoke the transform to decide — that is D26, and it is why
   validate.sh reads files rather than calling repairGlyphs. It checks: the
   directory exists; it holds at least one .md; every file is non-empty, valid
   UTF-8, and ends with a newline; and no file contains the code points the
   README excludes (em dash U+2014, en dash U+2013, hyphen U+2010, non-breaking
   hyphen U+2011, curly quotes U+2018 U+2019 U+201C U+201D, non-breaking space
   U+00A0, zero width space U+200B, ligatures U+FB01 U+FB02). Print the file
   count every run. Zero files exits NON-ZERO (D33).

═══ IF THE ROUND-TRIP TEST FAILS ON ITS FIRST RUN ═══
That is a FINDING, and it is the most valuable outcome this task can produce.
The golden documents were written by hand and were never tuned to pass, so a
failure means `heal` damages a well-formed document — a real criterion 10 bug.

- Do NOT edit any golden document to make the test pass.
- Do NOT weaken or delete the assertion.
- Record exactly which document, which line, and the before/after text.
- Write it to state/PARKED.md and name it in CURRENT.md as the next task.
- Commit only the parts that are green (the script, the corpus integrity checks,
  the step 0 corrections). Do NOT commit a red suite.
- Then STOP. Diagnosing a healing bug is a separate task with its own session.

═══ ONE PROJECT-LEVEL CHANGE IS AUTHORISED, AND ONLY THIS ONE ═══
The test bundle reads fixtures from its own resources because this repo sits
under ~/Documents and macOS TCC denies the xctest runner access to the source
tree (D28, D29). If the golden documents need the same treatment, you MAY add
Tests/Fixtures/golden-documents to the KitibTests resources in project.yml and
run `xcodegen generate`. That is the D29 precedent under the D16 grant and the
human has authorised it for this task.

Nothing else in project.yml is authorised. A new target, a build setting, an
Info.plist key, a capability, or a package dependency is RED — park it and stop.
Never hand-edit project.pbxproj, .xcodeproj, .xcworkspace, Info.plist,
.entitlements, or Package.swift.

═══ PROVE THE NEW TEST IS NOT VACUOUS ═══
A test over documents that are already correct passes whether or not it reads
anything. Prove it reads them, the same way the T3 baselines were proved:
append one junk line to ONE golden document, run the suite, confirm you get
exactly one failure naming that document, then RESTORE the file and confirm
`git status` is clean and the suite is green again. Paste both runs into the
commit message. Skipping this step means you have not tested the test.

═══ HOW TO RUN THE TESTS — GETTING THIS WRONG PASSES SILENTLY ═══
After creating ANY .swift file you MUST run `xcodegen generate` before testing.
This project uses XcodeGen with classic file references, NOT synchronized groups
— a new file on disk is in no target until you regenerate, so a test you wrote
but did not regenerate is simply not run and the suite exits 0 (D22).

  xcodegen generate
  export DEVELOPER_DIR="/Volumes/Samsung 990 2TB/Applications/Xcode.app/Contents/Developer"
  xcodebuild test -project Kitib.xcodeproj -scheme KitibTests \
    -destination 'platform=macOS,arch=arm64'

Never add -quiet: it hides the executed-test count. The count is 121 before you
start. It MUST be above 121 when you finish — if it is still 121, your test file
is not compiled and you have tested nothing. Xcode is on an external volume; if
it is unmounted, testing is impossible — stop and say so rather than skipping.

./scripts/validate.sh must still exit 0.

═══ CHANGE AUTHORITY ═══
GREEN  — internal implementation only → proceed, note it in the commit message.
AMBER  — changes acceptance criteria, a public interface, or adds a dependency
         → STOP, write to PARKED.md, commit what is green.
RED    — contradicts DECISIONS.md, changes the data model or file format,
         touches sync, or affects another feature → STOP, write to PARKED.md.
UNSURE — treat as RED.

═══ NEVER ═══
- Never edit a golden document, or anything under Tests/Fixtures/paste-healing/.
- Never weaken, skip, or delete a test or validator to go green.
- Never call PasteHealer from a shell script (D26).
- Never rename an existing entity. Exact identifiers only.

═══ BEFORE YOU EXIT — MANDATORY ═══
1. state/FEATURES.md — golden-roundtrip.sh and the golden corpus, with the test
   count. Mark ✅ only if the round trip is proved AND the seeded-defect check
   showed the test failing. Otherwise 🔨.
2. Append rulings to state/DECISIONS.md — in particular the split in this task:
   the round trip lives in XCTest because it needs the implementation, and the
   shell gate checks integrity only. Record it so nobody merges the two later.
3. Append any rejected approach to state/REJECTED.md.
4. Append any deferred idea to state/PARKED.md. Note that PARKED.md already
   carries a RED item about criterion 10 being narrower than it reads — that is
   the human's ruling to make, not yours. Do not act on it.
5. REWRITE state/CURRENT.md with the NEXT task and every gotcha you found.
6. git commit. The message must carry the verbatim output of the seeded-defect
   check and of step 0. A claim without its output is not a verification.

If you cannot complete this, write what you learned into CURRENT.md, commit the
partial work as 🔨, and stop cleanly. A clean stop beats a confident guess.
