captures/
*.png
!images/*.png
*.jpg
# gauntlet.sh writes its log to the repo root BEFORE its own preflight runs
# `git status --porcelain`. Untracked, that log makes the tree dirty and the
# script halts on itself every time. Must stay ignored.
gauntlet-*.log
Final Build/
_backup_originals/
GitHub/
.DS_Store
.build-app/
